<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:int="http://www.springframework.org/schema/integration"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns="http://www.springframework.org/schema/beans"
	xmlns:int-jms="http://www.springframework.org/schema/integration/jms"
	xmlns:int-stream="http://www.springframework.org/schema/integration/stream"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
			http://www.springframework.org/schema/beans/spring-beans.xsd
			http://www.springframework.org/schema/integration
			http://www.springframework.org/schema/integration/spring-integration.xsd
			http://www.springframework.org/schema/integration/jms
			http://www.springframework.org/schema/integration/jms/spring-integration-jms.xsd
			http://www.springframework.org/schema/integration/stream
			http://www.springframework.org/schema/integration/stream/spring-integration-stream.xsd">

	<int-stream:stdin-channel-adapter id="stdin" channel="stdinToJmsOutChannel"/>

	<int:chain id="jmsOutboundChain" input-channel="stdinToJmsOutChannel">
		<int:header-enricher>
			<int:header name="jms_replyTo" ref="replyQueue" />
		</int:header-enricher>
		<int-jms:outbound-channel-adapter destination="requestTopic" />
	</int:chain>

	<int-jms:message-driven-channel-adapter id="jmsReplyChannelAdapter" channel="jmsReplyChannel"
		destination="replyQueue"/>

	<int:channel id="jmsReplyChannel" />

	<int:aggregator id="jmsReplyChannelAggregator" input-channel="jmsReplyChannel" output-channel="stdOutChannelAdapter"
		group-timeout="40000"
		expire-groups-upon-timeout="false"
		send-partial-result-on-expiry="true"
		discard-channel="lateArrivalsLoggingChannelAdapter"
		correlation-strategy-expression="headers['jms_correlationId']"
		release-strategy-expression="size() == 2"/>

	<int:logging-channel-adapter id="lateArrivalsLoggingChannelAdapter" />

	<!-- Subscribers -->

	<int-jms:inbound-gateway id="upperCaseInboundGateway1" request-channel="upperCase" request-destination="requestTopic" />

	<int-jms:inbound-gateway id="upperCaseInboundGateway2" request-channel="upperCase" request-destination="requestTopic" />

	<int:transformer id="upperCaseTransformer" input-channel="upperCase" expression="payload.toUpperCase()" />

	<!-- Profiles -->

	<beans profile="default">

		<int-stream:stdout-channel-adapter id="stdOutChannelAdapter" append-newline="true"/>

	</beans>

	<beans profile="testCase">

		<int:bridge input-channel="stdOutChannelAdapter" output-channel="queueChannel"/>

		<int:channel id="queueChannel">
			<int:queue />
		</int:channel>

	</beans>

</beans>
